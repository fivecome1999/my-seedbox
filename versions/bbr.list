# BBR 变种清单
# ---------------------------------------------------------------------------
# 每行一个变种，字段用 | 分隔：
#   id | 显示名称 | 支持的系统(逗号分隔) | 内核算法名 | 简短说明
#
# - id           : 内部标识，同时对应 BBR/<id 首字母大写目录> 下的脚本与源码
#                  这里 id 用小写，安装脚本名按 BBRx.sh / BBRz.sh / BBRx_old.sh 约定
# - 支持的系统   : debian12 / debian13 等，用于在当前系统上过滤可选项
# - 内核算法名   : 模块向内核注册的拥塞控制算法名（对应 .c 里的 .name 字段），
#                  即 sysctl net.ipv4.tcp_congestion_control 要设的值。
#                  多数变种它与 id 相同；bbrx_old 因历史原因模块名带下划线、
#                  而算法名不能带下划线也不能与 bbrx 撞名，故单列为 bbrxold。
# - 说明         : 菜单里展示给用户的一句话
#
# 要新增一个 BBR 变种：在本文件加一行，并把对应的 .sh 安装脚本和 .c 源码
# 放进 BBR/<Name>/ 目录、推到仓库即可，主脚本无需改动。
# ---------------------------------------------------------------------------
bbrx|BBRx (通用优化版)|debian12,debian13|bbrx|平衡吞吐与延迟，适合大多数做种场景
bbrz|BBRz (激进吞吐版)|debian12,debian13|bbrz|更激进的抢占带宽策略，适合独享大带宽
bbrx_old|BBRx Old (旧版手感)|debian12,debian13|bbrxold|保留早期 BBRx 调参风格的怀旧版本
